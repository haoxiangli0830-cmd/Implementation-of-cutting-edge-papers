"""core — shared research library reused by every paper implementation.

Submodules:
    data        price / data loaders (with on-disk caching)
    backtest    vectorized portfolio backtest engine + cost models
    evaluation  performance metrics + the overfitting-rigor layer
"""
